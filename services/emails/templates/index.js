// export all templates
const contactTemplate = require("./contact-form");
const nuevaOportunidadTemplate = require("./nueva-oportunidad");
const newUserTemplate = require("./new-user");
const restorePassword = require("./restore-password");

module.exports = {
	contactTemplate,
	newUserTemplate,
	restorePassword,
	nuevaOportunidadTemplate
};
